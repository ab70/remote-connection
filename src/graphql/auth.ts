const getUserByCredentials = ({ email, password }) => {
    try {
        return { U: "AA", }

    } catch (err) {

    }
}
const createUser = ({ email, password }) => {
    try {
        return { U: "AA", }

    } catch (err) {

    }
}
export {
    createUser,
    getUserByCredentials
}